<div class="container">
  <h1>Tasks</h1>

<% @tasks.each do |t| %>
  <p><%= link_to t.title, task_path(t) %></p>
  <%= "Completed" %>
  <%= check_box("completed", t.id, {checked: t.completed}) %>
  <br>

<% end %>
<hr />
  <p><%= link_to "Create new task", new_path %></p>
</div>
